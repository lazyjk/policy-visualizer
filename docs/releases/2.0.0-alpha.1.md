# Policy Visualizer Release Notes — 2.0.0-alpha.1

## Release

- Version: `2.0.0-alpha.1`
- Release date: `2026-02-24`
- Release type: `alpha`
- Commit/tag: _(to be tagged after commit)_

## Summary

Release governance milestone. Aligns version metadata across the stack,
documents the SemVer and release gate policy, and clarifies the distinction
between the app version and the Policy IR schema version. No functional code
changes. Establishes the foundation for the 2.0.0 feature train.

## Highlights

- Version strings in `api/main.py` and `frontend/package.json` now track the release tag.
- SemVer bump rules, pre-release ladder, and release gates documented in `CLAUDE.md` § 12.
- App version and Policy IR schema version are now explicitly distinguished in `src/policy_ir.py`.

## Scope for This Release

- Included:
  - REL-201: SemVer rules and release gate policy documented in `CLAUDE.md`
  - REL-202: Backend app version bumped to `2.0.0-alpha.1` in `api/main.py`
  - REL-203: Frontend package version bumped to `2.0.0-alpha.1` in `frontend/package.json`
  - REL-204: App version vs Policy IR schema version distinction documented in `src/policy_ir.py` and `CLAUDE.md`
- Not included:
  - All feature work (export, annotations, UX, collapse) — deferred to subsequent alphas

## Breaking Changes

- None

## Features

### Export
- None (deferred to 2.0.0-alpha.2)

### Diagram UX
- None (deferred to 2.0.0-alpha.4)

### Policy/Compiler Pipeline
- None

### API
- FastAPI `info.version` now returns `2.0.0-alpha.1` (previously `1.0.0`)

## Fixes

- None

## Tests and Validation

- Test command(s):
  - `.venv/bin/pytest tests/`
- Result summary:
  - All 71 tests pass (no logic changes)
- Determinism checks:
  - No pipeline changes; determinism guarantee unchanged

## Known Issues

- None

## Upgrade Notes

- Docker:
  - `docker compose up --build`
- API/Frontend version alignment checks:
  - `api/main.py` → `version="2.0.0-alpha.1"`
  - `frontend/package.json` → `"version": "2.0.0-alpha.1"`

## Ticket Coverage

- REL-201: SemVer rules and release gates in `CLAUDE.md`
- REL-202: Backend app version aligned
- REL-203: Frontend package version aligned
- REL-204: App version vs IR schema version documented

## Deferred / Next Milestone

Items intentionally deferred:
- EXP-210, EXP-211, EXP-212: PNG/PDF export backend and frontend controls

Planned next target:
- `2.0.0-alpha.2` — Export Foundation

## Artifact Checklist

- [x] Release notes finalized
- [x] Version updated in backend metadata (`api/main.py`)
- [x] Version updated in frontend package (`frontend/package.json`)
- [ ] Tag created (`v2.0.0-alpha.1`)
- [ ] Docker build verified
- [x] Tests passing

---

## Optional Appendix: ISE Discovery Status (2.0 Track)

- Mapping matrix status: not started (ISE-250 deferred)
- Fixture plan status: not started (ISE-251 deferred)
- Open questions/risks: none tracked yet
- Recommendation for 2.1.0 import implementation: pending discovery work
