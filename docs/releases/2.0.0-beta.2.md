# Policy Visualizer Release Notes — 2.0.0-beta.2

## Release

- Version: `2.0.0-beta.2`
- Release date: `2026-02-25`
- Release type: `beta`
- Commit/tag: _(to be tagged after commit)_

## Summary

Hardening & Stabilization milestone. No new features. Confirms that the complete
alpha feature set (export, annotations, color picker, snap-to-grid) is free of
regressions, that the compiler pipeline remains deterministic, and that the Docker
packaging builds and starts cleanly from a cold state.

beta.1 (Vertical Collapse v1) has been deferred indefinitely — see release map.

## Highlights

- All 71 tests pass against the full alpha feature set with no regressions
- Docker cold-start validated: `docker compose up --build` produces a working app
  (API health ✅, frontend serving on :80 ✅)
- Determinism confirmed via `test_deterministic_compilation` and `test_deterministic_ids`
- API error boundary coverage confirmed (413/415/422/404 all tested)

## Scope for This Release

- Included:
  - Full 71-test sweep against post-alpha.4 codebase
  - Docker packaging cold-start validation
  - Release map updated: beta.1 deferred, beta.2 repositioned, UX-230/231/233 and
    COL-240/241 moved to post-GA deferred section
- Not included:
  - No new features
  - UX-230 (drag persistence), UX-231 (auto-align), UX-233 (multi-service tabs) — deferred post-GA
  - COL-240/241 (vertical collapse) — deferred TBD

## Breaking Changes

- None

## Features

- None (hardening milestone)

## Fixes

- None (no regressions found)

## Tests and Validation

- Test command(s):
  - `.venv/bin/pytest tests/`
- Result summary:
  - 71/71 passed in 1.69s — no failures, no warnings
- Determinism checks:
  - `test_deterministic_compilation` ✅ (flow IR)
  - `test_deterministic_ids` ✅ (policy IR)
- API boundary checks (from `test_api_routes.py`):
  - POST /api/services: 200, 413, 415, 422 (malformed XML, empty XML, no file) ✅
  - POST /api/flow: 200, 413, 415, 422, 404 (unknown service) ✅
- Docker packaging:
  - `docker compose build --no-cache` — both images built successfully ✅
  - `docker compose up -d` + `GET /api/health` → `{"status":"ok"}` ✅
  - Frontend serving HTML on :80 ✅

## Known Issues

- None

## Upgrade Notes

- Docker:
  - `docker compose up --build`
- API/Frontend version alignment checks:
  - `api/main.py` → `version="2.0.0-beta.2"`
  - `frontend/package.json` → `"version": "2.0.0-beta.2"`

## Ticket Coverage

- All REL, EXP, ANN, UX-232 tickets confirmed complete
- UX-230, UX-231, UX-233 moved to post-GA deferred
- COL-240, COL-241 moved to deferred (TBD)

## Deferred / Next Milestone

Items intentionally deferred:
- UX-230, UX-231, UX-233: remaining UX interaction items (post-GA)
- COL-240, COL-241: vertical collapse (TBD — preserved plan at `~/.claude/plans/cozy-enchanting-cocoa.md`)
- ANN-223: WYSIWYG annotation editor (post-GA)

Planned next target:
- `2.0.0-rc.1` — Release Candidate (feature freeze, docs, packaging runbook)

## Artifact Checklist

- [x] Release notes finalized
- [ ] Version updated in backend metadata (`api/main.py`)
- [ ] Version updated in frontend package (`frontend/package.json`)
- [ ] Tag created (`v2.0.0-beta.2`)
- [x] Docker build verified
- [x] Tests passing

---

## Optional Appendix: ISE Discovery Status (2.0 Track)

- Mapping matrix status: not started (ISE-250 deferred)
- Fixture plan status: not started (ISE-251 deferred)
- Open questions/risks: none tracked yet
- Recommendation for 2.1.0 import implementation: pending discovery work
