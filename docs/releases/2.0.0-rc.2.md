# Policy Visualizer Release Notes — 2.0.0-rc.2

## Release

- Version: `2.0.0-rc.2`
- Release date: `2026-02-25`
- Release type: `rc`
- Commit/tag: `v2.0.0-rc.2`

## Summary

Release Candidate 2. Pre-publication hygiene pass. Fixture files sanitized of
sensitive data, branding updated from "ClearPass Policy Visualizer" to "Policy
Visualizer" in preparation for a public repository release.

## Highlights

- Test fixture XML files sanitized: production credentials and domain names
  replaced with educational placeholder values
- App branding generalized: "ClearPass Policy Visualizer" → "Policy Visualizer"
  across UI, README, and API metadata — forward-compatible with planned ISE support
- All 71 tests pass against sanitized fixtures; no regressions

## Scope for This Release

- Included:
  - Fixture sanitization: `tests/fixtures/Service.xml`, `tests/fixtures/TacacsService.xml`
  - README title and intro updated
  - Browser tab title (`frontend/index.html`): `frontend` → `Policy Visualizer`
  - Upload panel heading (`UploadPanel.tsx`): `ClearPass Policy Visualizer` → `Policy Visualizer`
  - Upload drop zone and hint text: `ClearPass XML` → `service or policy XML file`
  - API metadata (`api/main.py`): title and description de-branded
  - Version bump: `2.0.0-rc.1` → `2.0.0-rc.2`
- Not included:
  - No new features
  - All deferred items remain deferred (see below)

## Breaking Changes

- None

## Features

- None (RC hardening only)

## Fixes

- None

## Tests and Validation

- Test command(s):
  - `.venv/bin/pytest tests/`
- Result summary:
  - 71/71 passed — no failures, no warnings
- Determinism checks:
  - `test_deterministic_compilation` ✅ (flow IR)
  - `test_deterministic_ids` ✅ (policy IR)
- Docker packaging:
  - `docker compose up --build` — both images built successfully ✅
  - `GET /api/health` → `{"status":"ok"}` ✅
  - Frontend serving on :80 ✅

## Known Issues

- None

## Upgrade Notes

- Docker:
  - `docker compose up --build`
- API/Frontend version alignment checks:
  - `api/main.py` → `version="2.0.0-rc.2"`
  - `frontend/package.json` → `"version": "2.0.0-rc.2"`

## Ticket Coverage

- All REL, EXP, ANN, UX-232 tickets confirmed complete (carried from rc.1)
- UX-230, UX-231, UX-233 remain deferred post-GA
- COL-240, COL-241 remain deferred TBD
- ISE-250/251/252 remain deferred (2.1.0 scope)

## Deferred / Next Milestone

Items intentionally deferred:
- UX-230, UX-231, UX-233: remaining UX interaction items (post-GA)
- COL-240, COL-241: vertical collapse (TBD — preserved plan at `~/.claude/plans/cozy-enchanting-cocoa.md`)
- ANN-223: WYSIWYG annotation editor (post-GA)
- ISE-250/251/252: ISE discovery and mapping (2.1.0 scope)

Planned next target:
- `2.0.0` — Public GA (tag and publish with final changelog)

## Artifact Checklist

- [x] Release notes finalized
- [x] Version updated in backend metadata (`api/main.py`)
- [x] Version updated in frontend package (`frontend/package.json`)
- [x] Tag created (`v2.0.0-rc.2`)
- [x] Docker build verified
- [x] Tests passing

---

## Optional Appendix: ISE Discovery Status (2.0 Track)

- Mapping matrix status: not started (ISE-250 deferred to 2.1.0)
- Fixture plan status: not started (ISE-251 deferred to 2.1.0)
- Open questions/risks: none tracked yet
- Recommendation for 2.1.0 import implementation: pending discovery work
