# Policy Visualizer Release Notes — 2.0.0-rc.1

## Release

- Version: `2.0.0-rc.1`
- Release date: `2026-02-25`
- Release type: `rc`
- Commit/tag: _(to be tagged after commit)_

## Summary

Release Candidate 1. Feature freeze. No new features. Promotes the beta.2 validated
state to release candidate status. Confirms docs and packaging are complete and
consistent for the 2.0.0 GA track.

## Highlights

- Feature freeze: all 2.0.0 scope features delivered and locked
- All 71 tests pass with no regressions
- Docker packaging validated (clean cold-start, health check, frontend serving)
- Docs verified: CLAUDE.md, release-map-2.0.md, and release notes are current

## Scope for This Release

- Included:
  - Version bump: `2.0.0-beta.2` → `2.0.0-rc.1` in `api/main.py` and `frontend/package.json`
  - Release map updated: beta.2 marked RELEASED, rc.1 marked NEXT
  - Release notes created (this file)
  - Full test suite re-run to confirm no drift since beta.2
- Not included:
  - No new features (feature freeze)
  - All deferred items remain deferred (see below)

## Breaking Changes

- None

## Features

- None (feature freeze milestone)

## Fixes

- None

## Tests and Validation

- Test command(s):
  - `.venv/bin/pytest tests/`
- Result summary:
  - 71/71 passed — no failures, no warnings
- Determinism checks:
  - `test_deterministic_compilation` ✅ (flow IR)
  - `test_deterministic_ids` ✅ (policy IR)
- API boundary checks (from `test_api_routes.py`):
  - POST /api/services: 200, 413, 415, 422 (malformed XML, empty XML, no file) ✅
  - POST /api/flow: 200, 413, 415, 422, 404 (unknown service) ✅
- Docker packaging (validated in beta.2, no changes since):
  - `docker compose build --no-cache` — both images built successfully ✅
  - `docker compose up -d` + `GET /api/health` → `{"status":"ok"}` ✅
  - Frontend serving HTML on :80 ✅

## Known Issues

- None

## Upgrade Notes

- Docker:
  - `docker compose up --build`
- API/Frontend version alignment checks:
  - `api/main.py` → `version="2.0.0-rc.1"`
  - `frontend/package.json` → `"version": "2.0.0-rc.1"`

## Ticket Coverage

- All REL, EXP, ANN, UX-232 tickets confirmed complete (carried from beta.2)
- UX-230, UX-231, UX-233 remain deferred post-GA
- COL-240, COL-241 remain deferred TBD
- ISE-250/251/252 remain deferred (2.1.0 scope)

## Deferred / Next Milestone

Items intentionally deferred:
- UX-230, UX-231, UX-233: remaining UX interaction items (post-GA)
- COL-240, COL-241: vertical collapse (TBD — preserved plan at `~/.claude/plans/cozy-enchanting-cocoa.md`)
- ANN-223: WYSIWYG annotation editor (post-GA)
- ISE-250/251/252: ISE discovery and mapping (2.1.0 scope)

Planned next target:
- `2.0.0` — Public GA (tag and publish with final changelog)

## Artifact Checklist

- [x] Release notes finalized
- [x] Version updated in backend metadata (`api/main.py`)
- [x] Version updated in frontend package (`frontend/package.json`)
- [ ] Tag created (`v2.0.0-rc.1`)
- [x] Docker build verified
- [x] Tests passing

---

## Optional Appendix: ISE Discovery Status (2.0 Track)

- Mapping matrix status: not started (ISE-250 deferred to 2.1.0)
- Fixture plan status: not started (ISE-251 deferred to 2.1.0)
- Open questions/risks: none tracked yet
- Recommendation for 2.1.0 import implementation: pending discovery work
